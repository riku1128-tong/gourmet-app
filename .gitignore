# 環境変数（API キーはここに置く。絶対にコミットしない）
.env
.env.*
!.env.example

# 依存パッケージ
node_modules/

# ビルド成果物（Vite 移行後）
dist/

# OS / エディタ
.DS_Store
Thumbs.db
*.log

# Claude Code のローカル起動設定（マシン固有パスを含む）
.claude/launch.json

# 名物抽出のサーバー側キャッシュ
.cache/

# Claude Code のスキル設定（マシン固有）
.claude/
skills-lock.json
