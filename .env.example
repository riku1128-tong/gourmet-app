# server.js が起動時に読み込むキー（このファイルを .env にコピーして値を入れる。.env はコミットされない）
HOTPEPPER_KEY=
ANTHROPIC_API_KEY=
# 同期（Supabase）。ローカルの Vite ビルド／dev で使う場合だけ（GitHub Pages は Actions の Secrets、Render は環境変数）
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
